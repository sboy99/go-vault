# Dialogs

Component: `src/components/ui/modal.tsx` (Kobalte Dialog)

- Overlay: `bg-black/50`
- Panel: `bg-default` / `bg-muted`, `border-default`, `rounded-md`, no shadow
- Title: 13px semibold `text-highlighted`
- Description: 12–13px `text-muted`
- Footer actions right-aligned, dense buttons

Use for destructive confirms (restore) and focused tasks.
