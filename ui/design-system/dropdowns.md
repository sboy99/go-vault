# Dropdowns

Component: `src/components/ui/dropdown.tsx` (Kobalte)

Composition (shadcn-like):

- `DropdownMenu`
- `DropdownMenuTrigger`
- `DropdownMenuContent`
- `DropdownMenuLabel` / `Item` / `Separator` / `Group`

Style: `bg-muted`, hairline border, `p-1`, item `px-2 py-1.5 text-[13px]`, highlight `bg-elevated`.

Theme picker is a specialized dropdown with boxed swatch cells.
