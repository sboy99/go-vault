# Shadows

Prefer **hairline borders** (`border-default`) over drop shadows.

- Cards, dropdowns, command menu: **no shadow**
- Overlay scrim: `bg-black/50` only
- Focus: 1px `outline` with `--ui-primary`, not a large ring

If elevation is required, use a lighter surface (`bg-elevated`) and a stronger border (`border-accented`).
