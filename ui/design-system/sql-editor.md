# SQL editor

**Not in product.** go-vault is a backup and restore control plane, not a query IDE.

If a future SQL surface is added, reuse:

- Dense monospace (`font-mono`)
- Near-black panel (`bg-muted`)
- Hairline borders
- Accent only for run/primary actions
