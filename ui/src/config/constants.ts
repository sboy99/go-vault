export const STORAGE_KEYS = {
  colorMode: 'go-vault.color-mode',
  apiToken: 'go-vault.api-token',
  primaryPalette: 'go-vault.primary-palette',
  neutralPalette: 'go-vault.neutral-palette',
  radius: 'go-vault.radius',
} as const

export const JOB_POLL_INTERVAL_MS = 2000

export const DEFAULT_PAGE_LIMIT = 50

export const APP_NAME = 'go-vault'
